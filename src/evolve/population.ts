/** @deprecated Import from `./archive.ts` — kept so older relative imports keep working. */
export {
  Archive,
  Archive as Population,
  type AddEvaluatedResult,
  type BotRecord,
} from './archive.ts'
